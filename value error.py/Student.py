class student :
  def __init__(self , name, email)
  self.names = names
  self.email = email
  self.validateNameEmail()
  self.validateName()
  
  def validateEmail(self):
    # search for the string has an @ sign
    pass 
  
  def validateName(self):
    if type(self ,names)is not str:
      raise validateError("{} is not a valid name".format(self.name)
jane = student("jane", janet)      